import React from 'react';

import {
    NavigationBar,
    Content
} from '@components/layout';

const Application = () => {
    return (
        <div>
            <NavigationBar />
            <Content />
        </div>
    );
};

export default Application;